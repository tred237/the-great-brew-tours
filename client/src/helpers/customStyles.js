export function buttonStyle(hover) {
    return (
        {    
            backgroundColor: "#525325", 
            border: "#525325",
            opacity: hover ? 0.8 : 1,
            color: "#FFFFFF"
        }
    )
}