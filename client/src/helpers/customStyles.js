export function submitButtonStyle(hover) {
    return (
        {    
            backgroundColor: "#525325", 
            border: "#525325",
            opacity: hover ? 0.8 : 1,
            color: "#FFFFFF"
        }
    )
}

export function deleteButtonStyle(hover) {
    return (
        {    
            backgroundColor: "#E84037", 
            border: "#E84037",
            opacity: hover ? 0.8 : 1,
            color: "#FFFFFF"
        }
    )
}